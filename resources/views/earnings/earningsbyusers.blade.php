<x-layouts.porto title="Earnings by Users per Month" header="Earnings by Users per Month" username={{$username}} profile_image={{$profile_image}}>
@foreach($users as $user)
<h2>Total earnings generated by {{$user->name}} per month: <strong>{{ $user->tasks()->onlyTrashed()->sum('budget'); }} USD</strong></h2>
@endforeach
</x-layouts.porto>